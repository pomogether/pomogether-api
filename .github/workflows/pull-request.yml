name: CI/CD Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
    build:
        uses: ./.github/workflows/build.yml@just-branch

    tests:
        needs:  build
        uses: ./.github/workflows/tests.yml@just-branch